# PalindroneCheckerApp
Start

Take input from user

Remove spaces / convert to lowercase (if string)

Reverse the input

Compare original input with reversed input

If both are equal → Display “Palindrome”

Else → Display “Not Palindrome”

End